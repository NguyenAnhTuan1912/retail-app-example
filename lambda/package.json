{
  "name": "demo-retail-lambda",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "package": "tsc && cd dist && zip -r ../lambda.zip ."
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
